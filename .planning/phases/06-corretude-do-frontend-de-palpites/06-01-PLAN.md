# Plan 06-01

## Goal

Eliminar concorrencia de autosave e refresh manual no fluxo de palpites.

## Tasks

1. Refatorar a `CupomView` para usar `single-flight` no autosave.
2. Recarregar `cupom`, `apostas` e `bracket` apos salvar sem sobrescrever edicoes locais mais novas.
3. Ajustar o feedback visual de salvamento para refletir o novo fluxo.
