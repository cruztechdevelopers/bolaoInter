# Verification

- `npm run build`
- `php artisan test --filter=BracketCupomApiTest`

Resultado:
- `CupomView` compilando com novo contrato de bracket
- proxima fase passa a aparecer com refetch automatico apos salvar
- resumo do podio do cupom vindo do backend
