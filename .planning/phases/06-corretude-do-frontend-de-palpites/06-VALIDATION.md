# Validation

- `npm run build`
- `php artisan test --filter=BracketCupomApiTest`

Criticos a observar:
- compilacao da `CupomView` apos mudanca de contrato do bracket
- regressao no endpoint privado do cupom
