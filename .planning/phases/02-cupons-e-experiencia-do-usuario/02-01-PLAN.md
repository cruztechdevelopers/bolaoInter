---
phase: 02-cupons-e-experiencia-do-usuario
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/database/migrations/2026_03_26_200000_add_telefone_to_usuarios_table.php
  - backend/database/migrations/2026_03_26_200001_add_valor_cupom_to_torneios_table.php
  - backend/app/Models/Usuario.php
  - backend/app/Models/Torneio.php
  - backend/app/Http/Requests/CadastrarUsuarioRequest.php
  - backend/app/Http/Controllers/AutenticacaoController.php
  - backend/app/Services/ServicoCheckout.php
  - backend/database/seeders/TorneioMockadoSeeder.php
  - backend/tests/Feature/CheckoutFluxoTest.php
  - backend/tests/Feature/TelefoneRegistroTest.php
  - frontend/src/tipos.ts
  - frontend/src/stores/autenticacao.ts
autonomous: true
requirements:
  - CUP-01
  - CUP-02
  - CUP-03
  - CUP-04

must_haves:
  truths:
    - "Campo telefone e aceito no cadastro e retornado no /usuario"
    - "Campo valor_cupom existe no torneio e e retornado pela API /torneio"
    - "Checkout cria pedido lendo valor_cupom do torneio ativo"
    - "Pagamento simulado muda status para pago e ativa cupom"
    - "Multiplos cupons podem ser criados pelo mesmo usuario"
  artifacts:
    - path: "backend/database/migrations/2026_03_26_200000_add_telefone_to_usuarios_table.php"
      provides: "Coluna telefone na tabela usuarios"
      contains: "telefone"
    - path: "backend/database/migrations/2026_03_26_200001_add_valor_cupom_to_torneios_table.php"
      provides: "Coluna valor_cupom na tabela torneios"
      contains: "valor_cupom"
    - path: "backend/tests/Feature/CheckoutFluxoTest.php"
      provides: "Testes do fluxo completo de checkout"
      min_lines: 40
    - path: "backend/tests/Feature/TelefoneRegistroTest.php"
      provides: "Testes do campo telefone no cadastro"
      min_lines: 20
  key_links:
    - from: "backend/app/Services/ServicoCheckout.php"
      to: "backend/app/Models/Torneio.php"
      via: "Leitura de valor_cupom do torneio publicado"
      pattern: "Torneio::.*valor_cupom"
    - from: "backend/app/Http/Controllers/AutenticacaoController.php"
      to: "backend/app/Models/Usuario.php"
      via: "Campo telefone no create do cadastro"
      pattern: "telefone"
---

<objective>
Ajustar o backend para suportar telefone no cadastro e valor_cupom no torneio, com testes cobrindo o fluxo de checkout completo.

Purpose: O frontend da Phase 2 depende de telefone no cadastro (D-20) e valor_cupom exibido no checkout (D-01, D-03). Esses ajustes de schema e logica precisam existir antes de qualquer reescrita de frontend.

Output: Duas migrations, models atualizados, servico de checkout lendo valor do torneio, testes de checkout e telefone passando, tipos TypeScript atualizados.
</objective>

<execution_context>
@C:/Projetos/Bolão Copa 2026/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/Bolão Copa 2026/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cupons-e-experiencia-do-usuario/02-CONTEXT.md
@.planning/phases/02-cupons-e-experiencia-do-usuario/02-RESEARCH.md

<interfaces>
<!-- Existing backend contracts the executor needs -->

From backend/app/Models/Usuario.php:
```php
protected $fillable = ['nome', 'email', 'password', 'perfil'];
```

From backend/app/Models/Torneio.php:
```php
protected $fillable = ['nome', 'edicao', 'status', 'data_inicio', 'data_fim'];
```

From backend/app/Services/ServicoCheckout.php:
```php
public function criarPedido(Usuario $usuario, ?float $valor = null): PedidoCheckout
{
    return PedidoCheckout::query()->create([
        'usuario_id' => $usuario->id,
        'valor' => $valor ?? 10,
        'status' => 'pendente',
        'referencia_checkout' => (string) Str::uuid(),
    ]);
}
```

From backend/app/Http/Requests/CadastrarUsuarioRequest.php:
```php
public function rules(): array
{
    return [
        'nome' => ['required', 'string', 'max:255'],
        'email' => ['required', 'email', 'max:255', 'unique:usuarios,email'],
        'password' => ['required', 'confirmed', Password::min(8)],
    ];
}
```

From backend/app/Http/Controllers/AutenticacaoController.php:
```php
$usuario = Usuario::query()->create([
    'nome' => $request->string('nome')->toString(),
    'email' => $request->string('email')->toString(),
    'password' => $request->string('password')->toString(),
    'perfil' => 'usuario',
]);
```

From frontend/src/tipos.ts:
```typescript
export type UsuarioAutenticado = {
  id: number; nome: string; email: string; perfil: PerfilUsuario
}
export type Torneio = {
  id: number; nome: string; edicao: string; status: string;
  data_inicio: string | null; grupos: Grupo[]; fases: Fase[];
  jogos: Jogo[]; regras_pontuacao: RegraPontuacao[];
  resultado_torneio?: ResultadoTorneio | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrations, models e ajustes backend (telefone + valor_cupom)</name>
  <files>
    backend/database/migrations/2026_03_26_200000_add_telefone_to_usuarios_table.php,
    backend/database/migrations/2026_03_26_200001_add_valor_cupom_to_torneios_table.php,
    backend/app/Models/Usuario.php,
    backend/app/Models/Torneio.php,
    backend/app/Http/Requests/CadastrarUsuarioRequest.php,
    backend/app/Http/Controllers/AutenticacaoController.php,
    backend/app/Services/ServicoCheckout.php,
    backend/database/seeders/TorneioMockadoSeeder.php,
    backend/tests/Feature/TelefoneRegistroTest.php,
    backend/tests/Feature/CheckoutFluxoTest.php
  </files>
  <read_first>
    backend/app/Models/Usuario.php,
    backend/app/Models/Torneio.php,
    backend/app/Http/Requests/CadastrarUsuarioRequest.php,
    backend/app/Http/Controllers/AutenticacaoController.php,
    backend/app/Services/ServicoCheckout.php,
    backend/database/seeders/TorneioMockadoSeeder.php,
    backend/tests/Feature/MvpFluxoApiTest.php
  </read_first>
  <behavior>
    - Test: POST /cadastro com telefone valido retorna 201 com usuario.telefone preenchido
    - Test: POST /cadastro sem telefone retorna 422 com erro de validacao
    - Test: GET /usuario retorna campo telefone do usuario autenticado
    - Test: POST /pedidos-checkout cria pedido com valor lido de torneio.valor_cupom (15.00 no teste)
    - Test: POST /pedidos-checkout/{id}/simular-pagamento muda status para 'pago' e cria cupom ativo
    - Test: Criar 2 pedidos + pagamentos para mesmo usuario resulta em 2 cupons distintos (CUP-04)
    - Test: Segundo pagamento do mesmo pedido retorna cupom existente (idempotencia)
  </behavior>
  <action>
    **Per D-20 (telefone obrigatorio no cadastro):**

    1. Criar migration `2026_03_26_200000_add_telefone_to_usuarios_table.php`:
       ```php
       Schema::table('usuarios', function (Blueprint $table) {
           $table->string('telefone', 20)->nullable()->after('email');
       });
       ```
       Campo nullable porque usuarios existentes (admin seeder) nao tem telefone.

    2. Atualizar `Usuario.php` $fillable: adicionar `'telefone'` apos `'email'`:
       ```php
       protected $fillable = ['nome', 'email', 'telefone', 'password', 'perfil'];
       ```

    3. Atualizar `CadastrarUsuarioRequest.php` rules - adicionar:
       ```php
       'telefone' => ['required', 'string', 'max:20'],
       ```

    4. Atualizar `AutenticacaoController::cadastrar` - adicionar telefone no create:
       ```php
       'telefone' => $request->string('telefone')->toString(),
       ```

    **Per D-01, D-03 (valor_cupom do torneio):**

    5. Criar migration `2026_03_26_200001_add_valor_cupom_to_torneios_table.php`:
       ```php
       Schema::table('torneios', function (Blueprint $table) {
           $table->decimal('valor_cupom', 10, 2)->default(10.00)->after('data_fim');
       });
       ```

    6. Atualizar `Torneio.php` $fillable: adicionar `'valor_cupom'`:
       ```php
       protected $fillable = ['nome', 'edicao', 'status', 'data_inicio', 'data_fim', 'valor_cupom'];
       ```
       Adicionar cast: `'valor_cupom' => 'decimal:2'` no metodo casts().

    7. Atualizar `ServicoCheckout::criarPedido` - ler valor do torneio ativo quando $valor nao fornecido:
       ```php
       use App\Models\Torneio;
       // ...
       $valorCupom = $valor ?? Torneio::where('status', 'publicado')->latest('id')->value('valor_cupom') ?? 10;
       ```
       Substituir `$valor ?? 10` por `$valorCupom`.

    8. Atualizar `TorneioMockadoSeeder` - adicionar `'valor_cupom' => 10.00` no updateOrCreate do torneio.

    **Testes (TDD - escrever primeiro, rodar RED, implementar, rodar GREEN):**

    9. Criar `backend/tests/Feature/TelefoneRegistroTest.php`:
       - `test_cadastro_com_telefone_retorna_201_com_telefone`
       - `test_cadastro_sem_telefone_retorna_422`
       - `test_usuario_autenticado_retorna_telefone`

    10. Criar `backend/tests/Feature/CheckoutFluxoTest.php`:
        - `test_criar_pedido_usa_valor_cupom_do_torneio`
        - `test_simular_pagamento_ativa_cupom`
        - `test_multiplos_cupons_por_usuario`
        - `test_pagamento_duplicado_retorna_cupom_existente`

    Rodar `php artisan migrate:fresh --seed` antes dos testes para ter schema limpo.
  </action>
  <verify>
    <automated>cd backend && php artisan migrate:fresh --seed && php artisan test --filter=TelefoneRegistroTest && php artisan test --filter=CheckoutFluxoTest</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "telefone" backend/database/migrations/2026_03_26_200000_add_telefone_to_usuarios_table.php
    - grep -q "'telefone'" backend/app/Models/Usuario.php
    - grep -q "'telefone' =>" backend/app/Http/Requests/CadastrarUsuarioRequest.php
    - grep -q "telefone" backend/app/Http/Controllers/AutenticacaoController.php
    - grep -q "valor_cupom" backend/database/migrations/2026_03_26_200001_add_valor_cupom_to_torneios_table.php
    - grep -q "'valor_cupom'" backend/app/Models/Torneio.php
    - grep -q "Torneio::" backend/app/Services/ServicoCheckout.php
    - grep -q "valor_cupom" backend/database/seeders/TorneioMockadoSeeder.php
    - php artisan test --filter=TelefoneRegistroTest exits 0
    - php artisan test --filter=CheckoutFluxoTest exits 0
  </acceptance_criteria>
  <done>
    Migrations de telefone e valor_cupom criadas, models e controllers ajustados, ServicoCheckout le valor do torneio, seeder atualizado, todos os testes passando.
  </done>
</task>

<task type="auto">
  <name>Task 2: Atualizar tipos TypeScript e store de autenticacao</name>
  <files>
    frontend/src/tipos.ts,
    frontend/src/stores/autenticacao.ts
  </files>
  <read_first>
    frontend/src/tipos.ts,
    frontend/src/stores/autenticacao.ts
  </read_first>
  <action>
    1. Em `frontend/src/tipos.ts`, adicionar campo `telefone` ao tipo `UsuarioAutenticado`:
       ```typescript
       export type UsuarioAutenticado = {
         id: number
         nome: string
         email: string
         telefone: string | null
         perfil: PerfilUsuario
       }
       ```

    2. Em `frontend/src/tipos.ts`, adicionar campo `valor_cupom` ao tipo `Torneio`:
       ```typescript
       export type Torneio = {
         id: number
         nome: string
         edicao: string
         status: string
         data_inicio: string | null
         data_fim: string | null
         valor_cupom: number
         grupos: Grupo[]
         fases: Fase[]
         jogos: Jogo[]
         regras_pontuacao: RegraPontuacao[]
         resultado_torneio?: ResultadoTorneio | null
       }
       ```

    3. Em `frontend/src/stores/autenticacao.ts`, adicionar ref `telefone`:
       - Adicionar `const telefone = ref<string | null>(null)` apos a ref `email`
       - Atualizar `aplicarUsuario` para incluir `telefone.value = usuario.telefone ?? null`
       - Atualizar `sair` para incluir `telefone.value = null`
       - Atualizar `cadastrar` para aceitar `telefoneUsuario: string` como parametro e envia-lo no corpo:
         ```typescript
         async function cadastrar(nomeUsuario: string, emailUsuario: string, telefoneUsuario: string, senha: string, confirmarSenha: string) {
           // ...
           corpo: {
             nome: nomeUsuario,
             email: emailUsuario,
             telefone: telefoneUsuario,
             password: senha,
             password_confirmation: confirmarSenha,
           },
         }
         ```
       - Exportar `telefone` no return do defineStore
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "telefone: string | null" frontend/src/tipos.ts
    - grep -q "valor_cupom: number" frontend/src/tipos.ts
    - grep -q "telefone" frontend/src/stores/autenticacao.ts
    - grep -q "telefoneUsuario" frontend/src/stores/autenticacao.ts
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    Tipos UsuarioAutenticado e Torneio atualizados com telefone e valor_cupom. Store de autenticacao gerencia telefone e envia no cadastro. Build TypeScript compila sem erros.
  </done>
</task>

</tasks>

<verification>
- `cd backend && php artisan test` passa sem erros
- `cd frontend && npx tsc --noEmit` passa sem erros
- `cd backend && php artisan migrate:fresh --seed` roda sem erros
</verification>

<success_criteria>
- Backend aceita telefone no cadastro e retorna em /usuario
- Torneio tem valor_cupom configuravel e retornado na API publica
- Checkout cria pedido com valor lido do torneio (nao hardcoded)
- Todos os testes existentes + novos passam
- Tipos frontend sincronizados com backend
</success_criteria>

<output>
After completion, create `.planning/phases/02-cupons-e-experiencia-do-usuario/02-01-SUMMARY.md`
</output>
