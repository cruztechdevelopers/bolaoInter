---
phase: 02-cupons-e-experiencia-do-usuario
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - frontend/src/stores/torneio.ts
  - frontend/src/components/SkeletonCard.vue
  - frontend/src/components/CupomCard.vue
  - frontend/src/components/InfoTorneio.vue
  - frontend/src/views/PainelView.vue
  - frontend/src/views/CupomView.vue
  - frontend/src/views/RankingView.vue
  - frontend/src/views/AdminPainelView.vue
autonomous: false
requirements:
  - CUP-04
  - CUP-05
  - UI-01
  - UI-02

must_haves:
  truths:
    - "Painel mostra lista de cupons do usuario com cards estilizados"
    - "Painel mostra empty state com CTA quando usuario nao tem cupons"
    - "Painel exibe info do torneio ativo abaixo dos cupons"
    - "Skeleton loading aparece enquanto dados carregam no painel"
    - "CupomView exibe tabs (Palpites, Ranking, Meus Resultados)"
    - "Ranking acessivel publicamente e destaca cupom quando acessado de dentro do cupom"
    - "Layout responsivo funciona em mobile e desktop"
    - "Admin visual alinhado ao tema do usuario final"
  artifacts:
    - path: "frontend/src/stores/torneio.ts"
      provides: "Store Pinia para cache de dados do torneio"
      exports: ["usarTorneioStore"]
    - path: "frontend/src/components/CupomCard.vue"
      provides: "Card de cupom com codigo, pontos, status e CTA"
      min_lines: 40
    - path: "frontend/src/views/PainelView.vue"
      provides: "Painel reescrito com header, grid, empty state, skeleton"
      min_lines: 100
    - path: "frontend/src/views/CupomView.vue"
      provides: "View de cupom com tabs"
      min_lines: 60
    - path: "frontend/src/views/RankingView.vue"
      provides: "Ranking com destaque de cupom"
      min_lines: 40
  key_links:
    - from: "frontend/src/views/PainelView.vue"
      to: "/api/cupons"
      via: "fetch lista de cupons do usuario"
      pattern: "requisicaoApi.*cupons"
    - from: "frontend/src/stores/torneio.ts"
      to: "/api/torneio"
      via: "fetch e cache do torneio publicado"
      pattern: "requisicaoApi.*torneio"
    - from: "frontend/src/views/CupomView.vue"
      to: "/api/cupons/{id}"
      via: "fetch dados do cupom individual"
      pattern: "requisicaoApi.*cupons/"
---

<objective>
Reescrever painel de cupons, view de cupom com tabs, ranking com destaque e polir admin.

Purpose: Completar a experiencia do usuario autenticado: ver seus cupons, navegar dentro de um cupom pelas tabs, acompanhar ranking global, e garantir responsividade em todos os breakpoints. Per D-04 a D-13, D-25, D-26, D-31.

Output: PainelView com cards, empty state e skeleton. CupomView com tabs. RankingView com destaque. TorneioStore compartilhado. AdminPainelView polida.
</objective>

<execution_context>
@C:/Projetos/Bolão Copa 2026/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/Bolão Copa 2026/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-cupons-e-experiencia-do-usuario/02-CONTEXT.md
@.planning/phases/02-cupons-e-experiencia-do-usuario/02-RESEARCH.md
@.planning/phases/02-cupons-e-experiencia-do-usuario/02-UI-SPEC.md
@.planning/phases/02-cupons-e-experiencia-do-usuario/02-01-SUMMARY.md
@.planning/phases/02-cupons-e-experiencia-do-usuario/02-02-SUMMARY.md

<interfaces>
<!-- Components and patterns from Plan 02 that this plan consumes -->

From frontend/src/composables/useToast.ts:
```typescript
export function useToast(): {
  toasts: Ref<Toast[]>;
  mostrar(tipo: 'sucesso' | 'erro', mensagem: string): void;
  remover(id: number): void;
}
```

From frontend/src/components/AvatarIniciais.vue:
```
Props: { nome: string; tamanho?: 'sm' | 'md' }
```

From frontend/src/components/AppHeader.vue:
```
Emits: 'abrir-modal-auth', 'toggle-menu'
Already rendered by App.vue — views don't need to import it
```

From frontend/src/tipos.ts:
```typescript
export type Cupom = {
  id: number; codigo: string; status: string;
  pedido_checkout_id: number | null;
  pontuacao?: PontuacaoCupom | null;
  eventos_pontuacao?: EventoPontuacao[];
}
export type PontuacaoCupom = {
  id: number; pontuacao_total: string; quantidade_placares_exatos: number;
  quantidade_classificados_corretos: number; quantidade_palpites_finais_corretos: number;
}
export type Torneio = {
  id: number; nome: string; edicao: string; status: string;
  valor_cupom: number; grupos: Grupo[]; fases: Fase[];
  jogos: Jogo[]; regras_pontuacao: RegraPontuacao[];
}
export type RankingItem = {
  id: number; pontuacao_total: string; quantidade_placares_exatos: number;
  cupom: { id: number; codigo: string; usuario: { nome: string } }
}
```

From backend API routes:
```
GET /api/cupons -> { cupons: Cupom[] }
GET /api/cupons/{id} -> { cupom: Cupom }
GET /api/torneio -> { torneio: Torneio }
GET /api/torneios/{id}/ranking -> { ranking: RankingItem[] }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Store do torneio, componentes de cupom e painel reescrito</name>
  <files>
    frontend/src/stores/torneio.ts,
    frontend/src/components/SkeletonCard.vue,
    frontend/src/components/CupomCard.vue,
    frontend/src/components/InfoTorneio.vue,
    frontend/src/views/PainelView.vue
  </files>
  <read_first>
    frontend/src/views/PainelView.vue,
    frontend/src/tipos.ts,
    frontend/src/services/api.ts,
    frontend/src/stores/autenticacao.ts,
    frontend/src/composables/useToast.ts,
    frontend/src/style.css,
    .planning/phases/02-cupons-e-experiencia-do-usuario/02-UI-SPEC.md
  </read_first>
  <action>
    **1. Criar `frontend/src/stores/torneio.ts`:**
    Store Pinia que cacheia dados do torneio publicado para evitar re-fetch em cada pagina.
    ```typescript
    import { ref } from 'vue'
    import { defineStore } from 'pinia'
    import { requisicaoApi } from '../services/api'
    import type { Torneio } from '../tipos'

    export const usarTorneioStore = defineStore('torneio', () => {
      const torneio = ref<Torneio | null>(null)
      const carregando = ref(false)
      const erro = ref<string | null>(null)

      async function carregar() {
        if (torneio.value) return // ja cacheado
        carregando.value = true
        erro.value = null
        try {
          const resp = await requisicaoApi<{ torneio: Torneio }>('/torneio')
          torneio.value = resp.torneio
        } catch (e) {
          erro.value = e instanceof Error ? e.message : 'Erro ao carregar torneio.'
        } finally {
          carregando.value = false
        }
      }

      async function recarregar() {
        torneio.value = null
        await carregar()
      }

      return { torneio, carregando, erro, carregar, recarregar }
    })
    ```

    **2. Criar `frontend/src/components/SkeletonCard.vue`:**
    - Placeholder animado no formato do card de cupom
    - Container: rounded-2xl border border-border bg-bg-card p-6
    - Conteudo: 3-4 divs com animate-pulse bg-bg-input rounded heights variadas (h-4, h-6, h-8)
    - Espacamento: space-y-4

    **3. Criar `frontend/src/components/CupomCard.vue` (per D-04):**
    - Props: `cupom: Cupom`
    - Card: rounded-2xl border border-border bg-bg-card p-6 hover:border-border-light transition-colors duration-150
    - Layout:
      - Topo: codigo do cupom (text-xs font-mono text-text-muted uppercase tracking-wider) + badge de status
      - Badge status: `ativo` = bg-primary/20 text-primary text-xs px-2 py-0.5 rounded-full uppercase tracking-wider. `pendente` = bg-warning/20 text-warning
      - Centro: pontos totais (text-3xl font-bold text-primary) com label "pontos" text-xs text-text-muted. Se nao tem pontuacao: "0" pontos
      - Info: "Placares exatos: {N}" text-sm text-text-secondary
      - Rodape: RouterLink to="/cupons/{id}" com texto "Fazer Palpites" — bg-primary/10 text-primary hover:bg-primary/20 text-sm font-medium px-4 py-2 rounded-lg transition w-full text-center block

    **4. Criar `frontend/src/components/InfoTorneio.vue` (per D-06):**
    - Usa usarTorneioStore()
    - Card: rounded-2xl border border-border bg-bg-card p-6
    - Titulo: "Torneio ativo" text-xs uppercase tracking-wider text-text-muted mb-2
    - Nome: torneio.nome + " — " + torneio.edicao (text-lg font-bold)
    - Status: badge com status do torneio
    - Info: numero de grupos, fases, jogos (text-sm text-text-secondary)
    - Data: "Inicio: {data_inicio formatada}" se disponivel
    - Skeleton: se carregando, mostrar divs animate-pulse

    **5. Reescrever `frontend/src/views/PainelView.vue` (per D-04, D-05, D-07, D-25):**

    Layout: max-w-5xl mx-auto.

    **Header do painel (per D-07):**
    - Container: rounded-2xl bg-gradient-to-r from-primary-dim/30 to-bg-card p-6 sm:p-8 mb-6
    - Saudacao: "Ola, {autenticacao.nome}" text-xl sm:text-2xl font-bold (per UI-SPEC greeting)
    - Subtitulo: "Cada cupom representa um conjunto independente de apostas." text-sm text-text-secondary (per UI-SPEC painel subtitle)
    - Botao: "Comprar novo cupom" RouterLink to="/checkout" — bg-primary hover:bg-primary-hover text-bg font-semibold px-6 py-2.5 rounded-xl transition. Alinhado a direita em desktop (flex justify-between items-start)

    **Lista de cupons:**
    - Loading: grid grid-cols-1 sm:grid-cols-2 gap-4 com 2 SkeletonCard
    - Com cupons: mesmo grid, renderizar CupomCard v-for
    - Empty state (per D-05): rounded-2xl border border-border bg-bg-card py-16 px-8 text-center
      - Titulo: "Nenhum cupom ainda" text-xl font-bold (per UI-SPEC)
      - Texto: "Compre seu primeiro cupom e comece a fazer seus palpites para o bolao." text-text-secondary (per UI-SPEC)
      - Botao: "Comprar primeiro cupom" RouterLink to="/checkout" — bg-primary hover:bg-primary-hover text-bg font-semibold px-8 py-3 rounded-xl text-lg transition

    **Info torneio (per D-06):**
    - Abaixo dos cupons, separado por mt-8
    - Renderizar InfoTorneio

    **Logica:**
    - onMounted: requisicaoApi<{ cupons: Cupom[] }>('/cupons') com loading state
    - usarTorneioStore().carregar() para InfoTorneio
    - usarAutenticacaoStore() para nome na saudacao
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "usarTorneioStore" frontend/src/stores/torneio.ts
    - grep -q "carregar" frontend/src/stores/torneio.ts
    - grep -q "animate-pulse" frontend/src/components/SkeletonCard.vue
    - grep -q "Fazer Palpites" frontend/src/components/CupomCard.vue
    - grep -q "pontuacao" frontend/src/components/CupomCard.vue
    - grep -q "InfoTorneio" frontend/src/components/InfoTorneio.vue
    - grep -q "Nenhum cupom ainda" frontend/src/views/PainelView.vue
    - grep -q "Comprar primeiro cupom" frontend/src/views/PainelView.vue
    - grep -q "Comprar novo cupom" frontend/src/views/PainelView.vue
    - grep -q "sm:grid-cols-2" frontend/src/views/PainelView.vue
    - grep -q "SkeletonCard" frontend/src/views/PainelView.vue
    - grep -q "CupomCard" frontend/src/views/PainelView.vue
    - grep -q "InfoTorneio" frontend/src/views/PainelView.vue
    - npm run build exits 0
  </acceptance_criteria>
  <done>
    TorneioStore com cache. SkeletonCard animado. CupomCard com codigo, pontos, status, CTA. InfoTorneio com dados do torneio. PainelView reescrita com header gradiente, grid responsivo, empty state CTA e skeleton loading.
  </done>
</task>

<task type="auto">
  <name>Task 2: CupomView com tabs, RankingView com destaque e admin polish</name>
  <files>
    frontend/src/views/CupomView.vue,
    frontend/src/views/RankingView.vue,
    frontend/src/views/AdminPainelView.vue
  </files>
  <read_first>
    frontend/src/views/CupomView.vue,
    frontend/src/views/RankingView.vue,
    frontend/src/views/AdminPainelView.vue,
    frontend/src/tipos.ts,
    frontend/src/services/api.ts,
    frontend/src/style.css,
    .planning/phases/02-cupons-e-experiencia-do-usuario/02-UI-SPEC.md,
    .planning/phases/02-cupons-e-experiencia-do-usuario/02-CONTEXT.md
  </read_first>
  <action>
    **1. Reescrever `frontend/src/views/CupomView.vue` (per D-11, D-12, D-26):**

    Layout: max-w-5xl mx-auto.

    **Header do cupom:**
    - Breadcrumb: "Meus Cupons" (link /painel) > "Cupom {codigo}" text-sm text-text-muted
    - Codigo: text-xl font-bold + badge status (mesmo estilo do CupomCard)

    **Tabs (per D-11):**
    - Container de tabs: flex border-b border-border overflow-x-auto (per D-26 para scroll horizontal mobile)
    - Tabs: "Palpites", "Ranking", "Meus Resultados"
    - Tab ativa: border-b-2 border-primary text-primary font-medium
    - Tab inativa: text-text-muted hover:text-text-secondary
    - Cada tab: px-4 py-3 text-sm whitespace-nowrap cursor-pointer transition

    **Tab Palpites:**
    - Sub-tabs: "Grupos", "Classificacao", "Mata-Mata", "Finais" (per D-11)
    - Sub-tab container: flex gap-2 mt-4 mb-6 overflow-x-auto
    - Sub-tab: px-3 py-1.5 rounded-lg text-xs font-medium. Ativa: bg-primary/20 text-primary. Inativa: bg-bg-input text-text-muted
    - Conteudo: placeholder "Palpites para esta secao estarao disponiveis na Phase 3." text-text-muted text-center py-12 (Palpites sao Phase 3, mas a estrutura de tabs deve existir agora)

    **Tab Ranking (per D-12):**
    - Carregar ranking: GET /api/torneios/{torneio.id}/ranking
    - Tabela: rounded-2xl border border-border bg-bg-card overflow-hidden
    - Header: bg-bg-input text-xs uppercase tracking-wider text-text-muted px-4 py-3
    - Colunas: #, Cupom, Usuario, Pontos, Exatos
    - Linha do cupom atual: bg-primary/10 border-l-2 border-primary (destaque per D-12)
    - Posicoes 1-3: icones/cores gold, silver, bronze (per UI-SPEC color)
    - Se ranking vazio: "Nenhum resultado disponivel ainda." text-text-muted text-center py-8

    **Tab Meus Resultados:**
    - Placeholder: "Historico de pontos deste cupom estara disponivel na Phase 4." text-text-muted text-center py-12
    - (Sera implementado com eventos_pontuacao na Phase 4)

    **Logica:**
    - Route params: useRoute().params.id para obter cupom_id
    - onMounted: fetch /api/cupons/{id} para dados do cupom
    - usarTorneioStore() para obter torneio.id (necessario para ranking)
    - Tab state: ref<'palpites' | 'ranking' | 'resultados'>('palpites')
    - Sub-tab state: ref<'grupos' | 'classificacao' | 'mata-mata' | 'finais'>('grupos')

    **2. Reescrever `frontend/src/views/RankingView.vue` (per D-12, D-13):**

    Ranking global acessivel publicamente (per D-13).

    Layout: max-w-5xl mx-auto.

    **Header:**
    - Titulo: "Ranking" text-2xl font-bold
    - Subtitulo: "Classificacao por cupom do torneio ativo." text-sm text-text-secondary

    **Tabela ranking:**
    - Mesma estrutura da tab Ranking do CupomView (extrair markup comum)
    - Container: rounded-2xl border border-border bg-bg-card overflow-hidden
    - Header: bg-bg-input text-xs uppercase tracking-wider text-text-muted
    - Colunas: Posicao (#), Cupom (codigo), Usuario (nome), Pontos (pontuacao_total), Exatos (quantidade_placares_exatos)
    - Posicoes 1-3: text-gold, text-silver, text-bronze
    - Query param opcional `?cupom={id}` para destacar um cupom especifico (vindo da tab dentro do CupomView via router link)
    - Linha destacada (quando cupom_id match): bg-primary/10 border-l-2 border-primary
    - Skeleton loading: 5 linhas animate-pulse enquanto carrega
    - Vazio: "Nenhum resultado disponivel. O ranking sera atualizado conforme os resultados dos jogos forem lancados." text-text-muted text-center py-12

    **Logica:**
    - usarTorneioStore().carregar() para obter torneio.id
    - fetch GET /api/torneios/{torneio.id}/ranking
    - useRoute().query.cupom para cupom destaque
    - Mobile responsive: tabela com overflow-x-auto para scroll horizontal em telas pequenas

    **3. Polir `frontend/src/views/AdminPainelView.vue` (per D-31):**
    - Ler o conteudo atual e aplicar o mesmo design system da Phase 2:
    - Cards: rounded-2xl border border-border bg-bg-card
    - Headers de secao: text-lg font-bold com text-text
    - Tabelas: rounded-2xl overflow-hidden border border-border, header bg-bg-input text-xs uppercase
    - Inputs: ja estilizados globalmente em style.css (manter)
    - Botoes: bg-primary hover:bg-primary-hover text-bg rounded-lg px-4 py-2 font-medium transition
    - Botoes danger: bg-danger/20 text-danger hover:bg-danger/30
    - Espacamento: gap-6 entre secoes, p-6 dentro dos cards
    - Responsive: grid-cols-1 sm:grid-cols-2 para cards de resumo
    - NAO alterar logica nem chamadas de API, apenas visual
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Palpites" frontend/src/views/CupomView.vue
    - grep -q "Ranking" frontend/src/views/CupomView.vue
    - grep -q "Meus Resultados" frontend/src/views/CupomView.vue
    - grep -q "Grupos" frontend/src/views/CupomView.vue
    - grep -q "overflow-x-auto" frontend/src/views/CupomView.vue
    - grep -q "bg-primary/10" frontend/src/views/CupomView.vue
    - grep -q "ranking" frontend/src/views/RankingView.vue
    - grep -q "torneios" frontend/src/views/RankingView.vue
    - grep -q "text-gold" frontend/src/views/RankingView.vue
    - grep -q "rounded-2xl" frontend/src/views/AdminPainelView.vue
    - npm run build exits 0
  </acceptance_criteria>
  <done>
    CupomView com 3 tabs (Palpites com sub-tabs placeholder, Ranking com destaque do cupom, Resultados placeholder). RankingView publica com destaque opcional. AdminPainelView polida no mesmo design system. Todos responsive e build compila.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacao visual e funcional da Phase 2</name>
  <what-built>
    Jornada completa do usuario: landpage com todas as secoes, login/cadastro via modal, checkout simulado com ativacao de cupom, painel de cupons com cards/empty state/skeleton, view de cupom com tabs, ranking publico com destaque, header responsivo com hamburger mobile, toast notifications, admin polida.
  </what-built>
  <how-to-verify>
    Iniciar o backend e frontend:
    1. `cd backend && php artisan serve --port=8888`
    2. `cd frontend && npm run dev`

    **Landpage (http://localhost:5173/):**
    3. Verificar secoes: hero com "Bolao Copa 2026", como funciona (4 passos), regras de pontuacao (vindo do banco), CTA "Compre seu cupom", FAQ (accordion), footer
    4. Verificar responsividade: reduzir viewport para < 640px, verificar layout empilhado

    **Auth modal:**
    5. Clicar "Comecar agora" ou "Cadastrar" no header — modal abre com tab "Criar conta"
    6. Preencher nome, email, telefone, senha, confirmar senha — submeter
    7. Toast "Conta criada! Bem-vindo ao Inter World Cup." aparece no canto top-right
    8. Redirecionado ao painel

    **Painel (http://localhost:5173/painel):**
    9. Se sem cupons: verificar empty state com "Nenhum cupom ainda" e botao "Comprar primeiro cupom"
    10. Verificar saudacao "Ola, {nome}" e botao "Comprar novo cupom"
    11. Verificar card info do torneio abaixo

    **Checkout (http://localhost:5173/checkout):**
    12. Card com nome do bolao, valor formatado (R$ 10,00), botao "Confirmar pagamento"
    13. Clicar confirmar: botao muda para "Processando...", apos sucesso redireciona ao painel com toast "Cupom liberado com sucesso!"
    14. Novo cupom aparece no grid do painel

    **Painel com cupom:**
    15. Card mostra codigo, pontos (0), status "ativo", botao "Fazer Palpites"
    16. Grid: 1 coluna mobile, 2 colunas desktop

    **CupomView (clicar "Fazer Palpites"):**
    17. Tabs: Palpites, Ranking, Meus Resultados
    18. Sub-tabs de Palpites: Grupos, Classificacao, Mata-Mata, Finais
    19. Tab Ranking: tabela (pode estar vazia)

    **Ranking (http://localhost:5173/ranking):**
    20. Acessivel sem autenticacao (deslogar e acessar)
    21. Tabela de ranking (pode estar vazia com mensagem)

    **Header responsivo:**
    22. Desktop: logo + links + dropdown do usuario (avatar com iniciais, nome)
    23. Mobile (< 640px): logo + hamburger. Clicar abre menu lateral com links e "Sair"

    **Admin (http://localhost:5173/admin):**
    24. Logar como admin (admin@interworldcup.local / 12345678)
    25. Verificar que o painel admin tem visual alinhado (cards arredondados, cores do tema)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` compila sem erros
- `cd backend && php artisan test` todos os testes passam
- Fluxo completo: landpage -> cadastro -> painel vazio -> checkout -> cupom ativo no painel -> view do cupom com tabs -> ranking
- Responsividade verificada em mobile e desktop
</verification>

<success_criteria>
- Painel mostra cupons em grid responsivo com skeleton durante loading
- Empty state com CTA funciona quando nao ha cupons
- CupomView tem tabs (Palpites com sub-tabs, Ranking com destaque, Resultados)
- Ranking publico acessivel sem login
- Admin visualmente alinhada ao tema
- Build compila sem erros
</success_criteria>

<output>
After completion, create `.planning/phases/02-cupons-e-experiencia-do-usuario/02-03-SUMMARY.md`
</output>
