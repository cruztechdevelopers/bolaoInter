---
phase: 02-cupons-e-experiencia-do-usuario
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/composables/useToast.ts
  - frontend/src/components/ToastContainer.vue
  - frontend/src/components/ModalAuth.vue
  - frontend/src/components/AvatarIniciais.vue
  - frontend/src/components/AppHeader.vue
  - frontend/src/components/MobileMenu.vue
  - frontend/src/views/InicioView.vue
  - frontend/src/views/CheckoutView.vue
  - frontend/src/App.vue
  - frontend/src/router/index.ts
autonomous: true
requirements:
  - CUP-01
  - CUP-03
  - UI-01

must_haves:
  truths:
    - "Landpage renderiza hero, como funciona, regras de pontuacao, CTA comprar cupom, FAQ e footer"
    - "Regras de pontuacao na landpage vem da API /torneio (nao hardcoded)"
    - "Login e cadastro funcionam via modal na landpage sem navegar para outra pagina"
    - "Checkout exibe valor do cupom vindo do torneio e confirma pagamento simulado"
    - "Apos pagamento simulado, usuario e redirecionado ao painel com toast de sucesso"
    - "Header fixo funciona em desktop e mobile com hamburger"
    - "Toast notifications aparecem no canto top-right e somem apos 4 segundos"
  artifacts:
    - path: "frontend/src/views/InicioView.vue"
      provides: "Landpage completa com todas as secoes"
      min_lines: 150
    - path: "frontend/src/views/CheckoutView.vue"
      provides: "Tela dedicada de checkout"
      min_lines: 50
    - path: "frontend/src/components/ModalAuth.vue"
      provides: "Modal de login e cadastro com tabs"
      min_lines: 80
    - path: "frontend/src/components/AppHeader.vue"
      provides: "Header fixo responsivo"
      min_lines: 60
    - path: "frontend/src/composables/useToast.ts"
      provides: "Sistema global de toast"
      exports: ["useToast"]
    - path: "frontend/src/components/ToastContainer.vue"
      provides: "Renderizacao dos toasts"
      min_lines: 20
  key_links:
    - from: "frontend/src/views/InicioView.vue"
      to: "/api/torneio"
      via: "fetch para carregar regras_pontuacao e valor_cupom"
      pattern: "requisicaoApi.*torneio"
    - from: "frontend/src/components/ModalAuth.vue"
      to: "frontend/src/stores/autenticacao.ts"
      via: "chamada de entrar() e cadastrar()"
      pattern: "autenticacao\\.(entrar|cadastrar)"
    - from: "frontend/src/views/CheckoutView.vue"
      to: "/api/pedidos-checkout"
      via: "POST para criar pedido e simular pagamento"
      pattern: "requisicaoApi.*pedidos-checkout"
---

<objective>
Reescrever a landpage, criar modal de autenticacao, tela de checkout e infraestrutura de UI (header, toast, avatar).

Purpose: Entregar a jornada publica do usuario: chegar na landpage, entender o bolao, cadastrar-se/entrar via modal, ir ao checkout e comprar o primeiro cupom. Per D-16, D-19, D-01, D-08, D-09.

Output: InicioView reescrita com todas as secoes, ModalAuth com tabs entrar/cadastro, CheckoutView dedicada, AppHeader responsivo, sistema de toast, navegacao atualizada.
</objective>

<execution_context>
@C:/Projetos/Bolão Copa 2026/.claude/get-shit-done/workflows/execute-plan.md
@C:/Projetos/Bolão Copa 2026/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-cupons-e-experiencia-do-usuario/02-CONTEXT.md
@.planning/phases/02-cupons-e-experiencia-do-usuario/02-RESEARCH.md
@.planning/phases/02-cupons-e-experiencia-do-usuario/02-UI-SPEC.md
@.planning/phases/02-cupons-e-experiencia-do-usuario/02-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that this plan consumes -->

From frontend/src/tipos.ts (after Plan 01):
```typescript
export type UsuarioAutenticado = {
  id: number; nome: string; email: string; telefone: string | null; perfil: PerfilUsuario
}
export type Torneio = {
  id: number; nome: string; edicao: string; status: string;
  data_inicio: string | null; data_fim: string | null; valor_cupom: number;
  grupos: Grupo[]; fases: Fase[]; jogos: Jogo[];
  regras_pontuacao: RegraPontuacao[]; resultado_torneio?: ResultadoTorneio | null;
}
export type RegraPontuacao = {
  id: number; fase_id: number | null; chave: string; nome: string;
  descricao: string | null; pontos: number; ativo: boolean;
}
export type PedidoCheckout = {
  id: number; valor: string; status: string;
  referencia_checkout: string | null; pago_at: string | null;
}
export type Cupom = {
  id: number; codigo: string; status: string;
  pedido_checkout_id: number | null;
  pontuacao?: PontuacaoCupom | null;
}
```

From frontend/src/stores/autenticacao.ts (after Plan 01):
```typescript
export const usarAutenticacaoStore = defineStore('autenticacao', () => {
  // refs: token, nome, email, telefone, perfil, carregando, erro
  // computed: estaAutenticado, eAdministrador
  // methods: entrar(email, senha), cadastrar(nome, email, telefone, senha, confirmarSenha), carregarUsuario(), sair()
})
```

From frontend/src/services/api.ts:
```typescript
export async function requisicaoApi<T>(caminho: string, opcoes?: OpcoesRequisicao): Promise<T>
// API_BASE_URL = 'http://127.0.0.1:8888/api'
```

From frontend/src/style.css (@theme):
```css
--color-bg: #0a0a0a; --color-bg-card: #141414; --color-bg-card-hover: #1a1a1a;
--color-bg-input: #1e1e1e; --color-border: #262626; --color-border-light: #333333;
--color-text: #f5f5f5; --color-text-secondary: #a3a3a3; --color-text-muted: #737373;
--color-primary: #10b981; --color-primary-hover: #059669; --color-primary-dim: #064e3b;
--color-danger: #ef4444; --color-warning: #f59e0b;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Infraestrutura de UI (toast, avatar, header, mobile menu) e roteador</name>
  <files>
    frontend/src/composables/useToast.ts,
    frontend/src/components/ToastContainer.vue,
    frontend/src/components/AvatarIniciais.vue,
    frontend/src/components/AppHeader.vue,
    frontend/src/components/MobileMenu.vue,
    frontend/src/App.vue,
    frontend/src/router/index.ts
  </files>
  <read_first>
    frontend/src/App.vue,
    frontend/src/router/index.ts,
    frontend/src/stores/autenticacao.ts,
    frontend/src/style.css,
    frontend/src/services/api.ts,
    .planning/phases/02-cupons-e-experiencia-do-usuario/02-UI-SPEC.md
  </read_first>
  <action>
    **1. Criar `frontend/src/composables/useToast.ts` (per D-28):**
    ```typescript
    import { ref } from 'vue'
    type Toast = { id: number; tipo: 'sucesso' | 'erro'; mensagem: string }
    const toasts = ref<Toast[]>([])
    let contadorId = 0
    export function useToast() {
      function mostrar(tipo: Toast['tipo'], mensagem: string) {
        const id = ++contadorId
        toasts.value.push({ id, tipo, mensagem })
        setTimeout(() => remover(id), 4000)
      }
      function remover(id: number) {
        toasts.value = toasts.value.filter(t => t.id !== id)
      }
      return { toasts, mostrar, remover }
    }
    ```
    Singleton via module-level ref (toasts compartilhado entre todos os consumidores).

    **2. Criar `frontend/src/components/ToastContainer.vue`:**
    - Container fixo top-right (fixed top-4 right-4 z-[60] flex flex-col gap-2)
    - Max 3 toasts visiveis
    - Cada toast: rounded-lg p-4, bg-primary/20 text-primary para sucesso, bg-danger/20 text-danger para erro
    - Transition com enter: translate-x-full -> translate-x-0 (300ms), leave: opacity-0 (200ms)
    - Botao X para fechar manual
    - Usar `<TransitionGroup>` do Vue

    **3. Criar `frontend/src/components/AvatarIniciais.vue` (per D-23):**
    - Props: `nome: string`, `tamanho?: 'sm' | 'md'` (sm=32px, md=36px)
    - Extrair iniciais: split por espacos, se 1 parte pega 2 primeiras letras, se 2+ pega primeira de cada extremo. Tudo uppercase.
    - Render: div com bg-primary text-bg rounded-full flex items-center justify-center font-bold text-xs (sm) ou text-sm (md)
    - Tamanhos: sm = `h-8 w-8`, md = `h-9 w-9`

    **4. Criar `frontend/src/components/AppHeader.vue` (per D-09, D-10):**
    - Header fixo: sticky top-0 z-50, height 64px desktop / 56px mobile
    - Background: bg-bg-card border-b border-border
    - Esquerda: logo (RouterLink to="/") com icone "IW" (span bg-primary rounded-lg h-9 w-9 text-bg font-bold) + textos "Inter World Cup" e "Copa 2026"
    - Centro/direita (desktop, hidden abaixo de sm): links "Painel" (/painel) e "Ranking" (/ranking) se autenticado. Se nao autenticado: "Entrar" e "Cadastrar" emitindo eventos `@abrir-modal-auth`
    - Direita (autenticado, desktop): UserDropdown inline — AvatarIniciais + nome. Click toggle dropdown com: nome, email, telefone (ou "Nao informado"), botao "Sair" (per D-22)
    - Direita (mobile, visivel abaixo de sm): AvatarIniciais (se autenticado) + botao hamburger (3 linhas SVG 24x24). Emite `@toggle-menu`
    - Se admin: link "Admin" (/admin) adicional
    - Props: nenhuma. Usa usarAutenticacaoStore(). Emits: 'abrir-modal-auth' com payload 'entrar' | 'cadastro', 'toggle-menu'

    **5. Criar `frontend/src/components/MobileMenu.vue` (per D-10):**
    - Props: `aberto: boolean`
    - Emits: `fechar`
    - Overlay: fixed inset-0 bg-black/50 z-50, click fecha
    - Menu: fixed top-0 right-0 h-full w-[280px] bg-bg-card border-l border-border z-50
    - Transition: transform translate-x-full -> translate-x-0, 300ms ease-in-out
    - Conteudo: user info no topo (avatar + nome + email), nav links (Painel, Ranking, Admin se admin), "Sair" no final
    - Se nao autenticado: links "Entrar" e "Cadastrar" que emitem evento e fecham menu
    - Fechar ao navegar (watch route)
    - Body scroll lock: `document.body.style.overflow = 'hidden'` quando aberto, restore ao fechar

    **6. Reescrever `frontend/src/App.vue`:**
    - Remover o header inline existente completamente
    - Estrutura:
      ```vue
      <template>
        <div class="min-h-screen flex flex-col">
          <AppHeader
            @abrir-modal-auth="abrirModalAuth"
            @toggle-menu="menuAberto = !menuAberto"
          />
          <MobileMenu :aberto="menuAberto" @fechar="menuAberto = false" />
          <main class="mx-auto w-full max-w-6xl flex-1 px-4 py-6 sm:px-6">
            <RouterView v-slot="{ Component }">
              <Transition name="fade" mode="out-in">
                <component :is="Component" @abrir-modal-auth="abrirModalAuth" />
              </Transition>
            </RouterView>
          </main>
          <ToastContainer />
          <ModalAuth v-if="modalAuthAberto" :tab-inicial="modalAuthTab" @fechar="modalAuthAberto = false" />
        </div>
      </template>
      ```
    - Script setup: importar AppHeader, MobileMenu, ToastContainer, ModalAuth. Refs: menuAberto, modalAuthAberto, modalAuthTab. Funcao abrirModalAuth(tab: 'entrar' | 'cadastro').
    - CSS no style: `.fade-enter-active, .fade-leave-active { transition: opacity 0.2s ease; } .fade-enter-from, .fade-leave-to { opacity: 0; }` (per D-30)
    - Manter onMounted com carregarUsuario()

    **7. Atualizar `frontend/src/router/index.ts`:**
    - Adicionar rota `/checkout`:
      ```typescript
      { path: '/checkout', name: 'checkout', component: () => import('../views/CheckoutView.vue'), meta: { requerAutenticacao: true } }
      ```
    - Redirecionar /entrar e /cadastro para landpage com query param (per D-21):
      ```typescript
      { path: '/entrar', redirect: { name: 'inicio', query: { modal: 'entrar' } } },
      { path: '/cadastro', redirect: { name: 'inicio', query: { modal: 'cadastro' } } },
      ```
    - Remover imports de EntrarView e CadastroView (agora inline no modal)
    - Atualizar guard: `validarRota` deve redirecionar para `{ name: 'inicio', query: { modal: 'entrar' } }` em vez de `{ name: 'entrar' }` (per pitfall 3 do RESEARCH.md)
    - Adicionar scrollBehavior ao createRouter: `scrollBehavior() { return { top: 0 } }`
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function useToast" frontend/src/composables/useToast.ts
    - grep -q "ToastContainer" frontend/src/components/ToastContainer.vue
    - grep -q "AvatarIniciais" frontend/src/components/AvatarIniciais.vue
    - grep -q "AppHeader" frontend/src/components/AppHeader.vue
    - grep -q "MobileMenu" frontend/src/components/MobileMenu.vue
    - grep -q "AppHeader" frontend/src/App.vue
    - grep -q "ToastContainer" frontend/src/App.vue
    - grep -q "ModalAuth" frontend/src/App.vue
    - grep -q "fade-enter-active" frontend/src/App.vue
    - grep -q "checkout" frontend/src/router/index.ts
    - grep -q "modal: 'entrar'" frontend/src/router/index.ts
    - npm run build exits 0
  </acceptance_criteria>
  <done>
    AppHeader responsivo com dropdown e hamburger. MobileMenu slide-in. Toast system global. AvatarIniciais com iniciais do nome. App.vue usa novos componentes com fade transition. Router com checkout, redirecionamento de /entrar e /cadastro, guard atualizado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Landpage completa, modal de autenticacao e tela de checkout</name>
  <files>
    frontend/src/components/ModalAuth.vue,
    frontend/src/views/InicioView.vue,
    frontend/src/views/CheckoutView.vue
  </files>
  <read_first>
    frontend/src/views/InicioView.vue,
    frontend/src/stores/autenticacao.ts,
    frontend/src/services/api.ts,
    frontend/src/tipos.ts,
    frontend/src/composables/useToast.ts,
    frontend/src/style.css,
    .planning/phases/02-cupons-e-experiencia-do-usuario/02-UI-SPEC.md,
    .planning/phases/02-cupons-e-experiencia-do-usuario/02-CONTEXT.md
  </read_first>
  <action>
    **1. Criar `frontend/src/components/ModalAuth.vue` (per D-19):**
    - Props: `tabInicial: 'entrar' | 'cadastro'`
    - Emits: `fechar`
    - Overlay: fixed inset-0 bg-black/60 backdrop-blur-sm z-50, click fecha
    - Modal desktop: max-w-md mx-auto mt-20, rounded-2xl bg-bg-card p-8
    - Modal mobile: fixed bottom-0 left-0 right-0 rounded-t-2xl bg-bg-card p-6 (bottom sheet)
    - Transitions: overlay fade 200ms, modal slide-up 300ms (mobile) / scale-in 200ms (desktop)
    - Tab selector: dois botoes "Entrar" e "Criar conta", aba ativa com border-b-2 border-primary text-primary
    - Formulario Entrar: email + senha + botao "Entrar" (full width bg-primary rounded-lg py-2.5)
    - Formulario Cadastro: nome + email + telefone + senha + confirmar senha + botao "Criar conta" (per D-20)
    - Usa usarAutenticacaoStore().entrar() e .cadastrar()
    - Usa useToast().mostrar('sucesso', ...) apos sucesso (per UI-SPEC copies: "Login realizado com sucesso." / "Conta criada! Bem-vindo ao Inter World Cup.")
    - Usa useToast().mostrar('erro', ...) em catch (per UI-SPEC: "Email ou senha incorretos. Verifique seus dados e tente novamente.")
    - Apos login/cadastro sucesso: emitir 'fechar', navegar para /painel via useRouter()
    - Focus trap: onMounted, querySelectAll focusable elements, Tab/Shift+Tab cicla dentro do modal
    - Body scroll lock: document.body.style.overflow = 'hidden' onMounted, restore onUnmounted
    - Escape fecha modal

    **2. Reescrever `frontend/src/views/InicioView.vue` (per D-16, D-17, D-18):**

    Landpage completa com secoes. Content max-w-6xl mx-auto px-4 sm:px-6 py-8 space-y-20.

    **Secao Hero (per D-16):**
    - Container: rounded-3xl bg-gradient-to-br from-primary-dim/30 to-bg-card p-8 sm:p-16 text-center
    - Eyebrow: "BOLAO ONLINE" uppercase tracking-widest text-xs text-primary
    - Titulo: "Bolao Copa 2026" text-4xl sm:text-5xl font-bold. Palavra "Online" em span text-primary (per UI-SPEC hero title)
    - Subtitulo: "Compre seus cupons, faca seus palpites e acompanhe o ranking em tempo real." text-text-secondary text-sm sm:text-base max-w-xl mx-auto
    - CTA: se autenticado -> "Ir para Meus Cupons" (link /painel). Se nao autenticado -> "Comecar agora" abre modal cadastro (emit 'abrir-modal-auth' com 'cadastro')
    - Botao: bg-primary hover:bg-primary-hover text-bg font-semibold px-8 py-3 rounded-xl transition

    **Secao Como Funciona:**
    - Titulo: "Como funciona" text-xl sm:text-2xl font-bold text-center
    - Grid: grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4
    - 4 cards com icone SVG inline (numeros 1-4 em circulos bg-primary/20), titulo e descricao:
      1. "Cadastre-se" / "Crie sua conta em segundos e entre para o bolao."
      2. "Compre um cupom" / "Cada cupom custa R$ {valor_cupom} e representa uma entrada independente."
      3. "Faca seus palpites" / "Aposte nos placares, classificados, artilheiro e mais."
      4. "Acompanhe o ranking" / "Veja sua posicao em tempo real conforme os resultados saem."
    - Card: rounded-2xl bg-bg-card border border-border p-6

    **Secao Regras de Pontuacao (per D-17):**
    - Titulo: "Regras de pontuacao" text-xl sm:text-2xl font-bold text-center
    - Carregar do /api/torneio: `torneio.regras_pontuacao.filter(r => r.ativo)`
    - Grid: sm:grid-cols-2 lg:grid-cols-3 gap-4
    - Card por regra: rounded-xl bg-bg-card border border-border p-5. Nome (text-sm font-medium text-text), pontos (text-2xl font-bold text-primary), descricao (text-xs text-text-muted)
    - Skeleton loading: 6 SkeletonCard enquanto carrega (ou divs animate-pulse bg-bg-input rounded-xl h-24)

    **Secao Compre Seu Cupom (per D-18):**
    - Container: rounded-3xl border border-border bg-gradient-to-br from-bg-card to-bg p-8 sm:p-12 text-center
    - Titulo: "Compre seu cupom" text-xl sm:text-2xl font-bold
    - Texto: "Participe do bolao por apenas R$ {valor_cupom formatado com Intl.NumberFormat}. Cada cupom e independente." text-text-secondary
    - CTA: se autenticado -> "Comprar novo cupom" link /checkout. Se nao -> "Cadastre-se para comprar" abre modal cadastro
    - Valor formatado com: `new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(torneio.valor_cupom)`

    **Secao FAQ (per D-16 - FAQ estatico hardcoded):**
    - Titulo: "Perguntas frequentes" text-xl sm:text-2xl font-bold text-center
    - Container: max-w-3xl mx-auto
    - Items accordion (5-6 perguntas). Cada item: border-b border-border py-4. Pergunta como button full width text-left flex justify-between. Resposta com v-show e transition.
    - Perguntas sugeridas:
      1. "O que e um cupom?" / "Cada cupom e uma entrada independente no bolao. Voce pode comprar quantos quiser, e cada um tera seus proprios palpites e pontuacao."
      2. "Como funciona a pontuacao?" / "Os pontos sao calculados com base nas regras configuradas para o torneio. Acertar o placar exato vale mais pontos."
      3. "Posso ter mais de um cupom?" / "Sim! Cada cupom funciona de forma independente com seu proprio conjunto de apostas e posicao no ranking."
      4. "Ate quando posso fazer meus palpites?" / "Cada fase e rodada tem um prazo de fechamento. Apos o prazo, nao e possivel criar ou editar apostas."
      5. "Como funciona o ranking?" / "O ranking e por cupom. A posicao e determinada pela pontuacao total, com desempate por placares exatos acertados."

    **Secao Footer:**
    - Full width bg-bg-card border-t border-border
    - Container: max-w-6xl mx-auto px-4 sm:px-6 py-8
    - Texto: "Inter World Cup" + copyright ano atual. text-sm text-text-muted text-center

    **Logica:**
    - onMounted: fetch /api/torneio com requisicaoApi, guardar em ref torneio
    - Verificar query param `modal` da route: se presente, abrir modal auth com tab correspondente (para suportar redirect de /entrar e /cadastro)
    - Emitir 'abrir-modal-auth' para o App.vue abrir o ModalAuth (ou usar query param watch)

    NOTA: A landpage e a InicioView. O App.vue ja envolve com AppHeader. Porem na landpage, para visitantes nao autenticados, o AppHeader ja mostra "Entrar"/"Cadastrar". Entao a landpage nao precisa de seu proprio header.

    **3. Criar `frontend/src/views/CheckoutView.vue` (per D-01, D-02, D-03):**
    - Layout: flex items-center justify-center min-h-[60vh]
    - Card: max-w-md w-full rounded-2xl border border-border bg-bg-card p-8
    - Conteudo:
      - Icone trofeu SVG no topo (48x48, text-primary)
      - Titulo: "Confirmar compra" text-xl font-bold text-center
      - Info: nome do bolao ("Inter World Cup — Copa 2026") text-text-secondary text-center
      - Valor: R$ {valor_cupom formatado} text-3xl font-bold text-primary text-center my-6
      - Botao: "Confirmar pagamento" full width bg-primary hover:bg-primary-hover text-bg font-semibold py-3 rounded-xl transition
      - Link cancelar: "Cancelar compra" text-sm text-text-muted hover:text-danger text-center mt-4, navega para /painel
    - Logica:
      - onMounted: fetch /api/torneio para obter valor_cupom e nome
      - Ao clicar "Confirmar pagamento":
        1. Desabilitar botao, texto muda para "Processando..." (per UI-SPEC buttons)
        2. POST /api/pedidos-checkout -> obtem pedido
        3. POST /api/pedidos-checkout/{id}/simular-pagamento -> obtem cupom
        4. useToast().mostrar('sucesso', 'Cupom liberado com sucesso!') (per D-02 e UI-SPEC)
        5. router.push({ name: 'painel' })
      - Em caso de erro: useToast().mostrar('erro', 'Nao foi possivel concluir a compra. Tente novamente em alguns instantes.') (per UI-SPEC)
      - Skeleton/loading: enquanto carrega dados do torneio, mostrar skeleton do card
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ModalAuth" frontend/src/components/ModalAuth.vue
    - grep -q "Bolao Copa 2026" frontend/src/views/InicioView.vue
    - grep -q "Como funciona" frontend/src/views/InicioView.vue
    - grep -q "regras_pontuacao" frontend/src/views/InicioView.vue
    - grep -q "Compre seu cupom" frontend/src/views/InicioView.vue
    - grep -q "Perguntas frequentes" frontend/src/views/InicioView.vue
    - grep -q "Confirmar pagamento" frontend/src/views/CheckoutView.vue
    - grep -q "pedidos-checkout" frontend/src/views/CheckoutView.vue
    - grep -q "simular-pagamento" frontend/src/views/CheckoutView.vue
    - grep -q "Cupom liberado com sucesso" frontend/src/views/CheckoutView.vue
    - npm run build exits 0
  </acceptance_criteria>
  <done>
    Landpage completa com hero, como funciona, regras do banco, CTA, FAQ e footer. ModalAuth com login/cadastro via tabs, focus trap e scroll lock. CheckoutView com confirmacao de pagamento simulado, redirecionamento e toast. Build compila sem erros.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` passa sem erros
- Abrir http://localhost:5173/ mostra landpage completa com todas as secoes
- Clicar "Comecar agora" abre modal de cadastro
- Preencher cadastro (com telefone) e submeter cria conta e redireciona ao painel
- Navegar para /checkout mostra tela de confirmacao com valor do torneio
- Confirmar pagamento redireciona ao painel com toast "Cupom liberado com sucesso!"
- Header fixo visivel em todas as paginas autenticadas
- Em viewport mobile (< 640px): hamburger visivel, menu lateral funciona
</verification>

<success_criteria>
- Landpage renderiza 6 secoes (hero, como funciona, regras, CTA, FAQ, footer)
- Regras de pontuacao carregadas da API (nao hardcoded)
- Modal de auth funciona com tabs entrar/cadastro incluindo campo telefone
- Checkout exibe valor do torneio e completa fluxo simulado
- Toast aparece e some apos 4s
- Header responsivo com dropdown desktop e hamburger mobile
- Build TypeScript compila sem erros
</success_criteria>

<output>
After completion, create `.planning/phases/02-cupons-e-experiencia-do-usuario/02-02-SUMMARY.md`
</output>
