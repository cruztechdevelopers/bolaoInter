---
phase: 01-fundacao-do-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/composer.json
  - backend/.env.example
  - backend/routes/api.php
  - backend/config/cors.php
  - frontend/package.json
  - frontend/vite.config.js
  - frontend/src/main.js
  - frontend/src/router/index.js
  - frontend/src/stores/autenticacao.js
  - frontend/src/App.vue
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - UI-03
user_setup: []
must_haves:
  truths:
    - O projeto possui aplicações Laravel e Vue inicializadas e executáveis
    - O backend aponta corretamente para o banco local interWordCup
    - O frontend possui roteamento base e estrutura inicial para área pública e autenticada
  artifacts:
    - path: backend/composer.json
      provider: laravel
      min_lines: 20
    - path: frontend/package.json
      provider: vue
      min_lines: 20
    - path: frontend/src/router/index.js
      provider: planejamento
      min_lines: 15
  key_links:
    - backend/.env.example define a conexão esperada para o MySQL local
    - frontend/src/main.js registra Pinia e Router antes de montar a aplicação
---

<objective>
Fundar a estrutura técnica do projeto com um backend Laravel e um frontend Vue, ambos prontos para sustentar o restante da Fase 1.

Purpose: Garantir uma base consistente antes de modelar domínio, autenticação e administração.
Output: Estrutura inicial dos aplicativos, configuração de banco e navegação base do frontend.
</objective>

<execution_context>
@C:/Projetos/Bolão Copa 2026/.codex/get-shit-done/workflows/execute-plan.md
@C:/Projetos/Bolão Copa 2026/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-fundacao-do-mvp/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inicializar os aplicativos backend e frontend</name>
  <files>backend/composer.json, frontend/package.json</files>
  <read_first>.planning/phases/01-fundacao-do-mvp/01-RESEARCH.md, .planning/ROADMAP.md</read_first>
  <action>Criar a aplicação Laravel em `backend/` e a aplicação Vue 3 com Vite em `frontend/`. Não misture os dois runtimes na raiz. Use o nome do domínio em português no código criado manualmente a partir daqui, mas respeite a estrutura padrão dos frameworks para evitar atrito com ferramentas e documentação.</action>
  <verify>Test-Path backend/composer.json; Test-Path frontend/package.json</verify>
  <acceptance_criteria>
    - `backend/composer.json` existe
    - `frontend/package.json` existe
    - a raiz do repositório mantém `.planning/`, `backend/` e `frontend/` separados
  </acceptance_criteria>
  <done>Backend e frontend inicializados em diretórios independentes</done>
</task>

<task type="auto">
  <name>Task 2: Configurar base técnica de execução e integração local</name>
  <files>backend/.env.example, backend/routes/api.php, backend/config/cors.php, frontend/vite.config.js</files>
  <read_first>backend/.env.example, backend/config/cors.php</read_first>
  <action>Configurar o Laravel para o banco local `interWordCup` com host `127.0.0.1`, usuário `root` e senha vazia no arquivo de exemplo e na orientação do projeto. Ajustar CORS para permitir o frontend local durante o desenvolvimento. Configurar o frontend com Vite de forma compatível com consumo de API do backend.</action>
  <verify>Select-String -Path backend/.env.example -Pattern 'DB_DATABASE=interWordCup'; Test-Path backend/config/cors.php</verify>
  <acceptance_criteria>
    - `backend/.env.example` contém `DB_DATABASE=interWordCup`
    - `backend/.env.example` contém `DB_HOST=127.0.0.1`
    - CORS do backend permite integração local com o frontend
  </acceptance_criteria>
  <done>Configuração local pronta para backend e frontend conversarem</done>
</task>

<task type="auto">
  <name>Task 3: Criar esqueleto de navegação do frontend</name>
  <files>frontend/src/main.js, frontend/src/router/index.js, frontend/src/stores/autenticacao.js, frontend/src/App.vue</files>
  <read_first>frontend/src/main.js, frontend/src/App.vue</read_first>
  <action>Registrar Vue Router e Pinia. Criar rotas mínimas para página pública, login, painel do usuário e área administrativa. Criar store inicial de autenticação para centralizar estado de sessão desde o começo. O objetivo não é terminar a autenticação agora, e sim deixar a espinha dorsal do app pronta para os próximos planos.</action>
  <verify>Test-Path frontend/src/router/index.js; Test-Path frontend/src/stores/autenticacao.js</verify>
  <acceptance_criteria>
    - `frontend/src/main.js` monta a aplicação com Router e Pinia
    - `frontend/src/router/index.js` define rotas pública, autenticada e administrativa
    - `frontend/src/stores/autenticacao.js` existe
  </acceptance_criteria>
  <done>Frontend possui base de navegação e estado para receber os módulos da fase</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `php artisan --version` executa em `backend/`
- [ ] `npm run build` executa em `frontend/`
- [ ] estrutura de diretórios do projeto está coerente com o roadmap
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Backend e frontend existem, inicializam e suportam a continuação da fase
  </success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-do-mvp/01-01-SUMMARY.md`
</output>
