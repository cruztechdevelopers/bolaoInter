---
phase: 01-fundacao-do-mvp
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - backend/app/Models/Usuario.php
  - backend/app/Models/Cupom.php
  - backend/app/Models/Torneio.php
  - backend/database/migrations
  - backend/database/seeders/DatabaseSeeder.php
  - backend/database/seeders/UsuarioAdministradorSeeder.php
  - backend/database/seeders/TorneioMockadoSeeder.php
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - PONT-01
  - ADM-02
  - ADM-03
user_setup: []
must_haves:
  truths:
    - O schema principal do domínio existe em migrations e consegue ser aplicado no MySQL local
    - As regras de pontuação ficam persistidas em banco e não em código
    - O sistema pode ser populado com dados mockados por seeders
  artifacts:
    - path: backend/database/migrations
      provider: laravel
      min_lines: 50
    - path: backend/database/seeders/TorneioMockadoSeeder.php
      provider: seed
      min_lines: 30
    - path: backend/app/Models/Torneio.php
      provider: eloquent
      min_lines: 20
  key_links:
    - migrations criam a tabela `regras_pontuacao`
    - seeders populam torneio, grupos, seleções, fases, rodadas, jogos e regras de pontuação
---

<objective>
Modelar o domínio central do MVP em Laravel com migrations, models e seeders.

Purpose: Fechar a estrutura de dados antes da autenticação administrativa e da interface de gestão.
Output: Schema inicial, modelos Eloquent do domínio e massa de dados mockada para desenvolvimento.
</objective>

<execution_context>
@C:/Projetos/Bolão Copa 2026/.codex/get-shit-done/workflows/execute-plan.md
@C:/Projetos/Bolão Copa 2026/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-fundacao-do-mvp/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migrations do domínio principal</name>
  <files>backend/database/migrations</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Criar as migrations para `usuarios`, `pedidos_checkout`, `cupons`, `torneios`, `grupos`, `selecoes`, `jogadores`, `fases`, `rodadas`, `jogos`, `resultados_jogos`, `regras_pontuacao`, `apostas`, `logs_apostas`, `eventos_pontuacao` e `pontuacoes_cupons`. Preservar os campos estruturais esperados pelo Laravel e manter o domínio em português. Garanta foreign keys e índices necessários desde a primeira versão.</action>
  <verify>Get-ChildItem backend/database/migrations | Measure-Object</verify>
  <acceptance_criteria>
    - existe migration para todas as tabelas do DER definido
    - a tabela `regras_pontuacao` existe nas migrations
    - `usuarios` usa colunas compatíveis com autenticação Laravel
  </acceptance_criteria>
  <done>Schema inicial completo e coerente com o DER do projeto</done>
</task>

<task type="auto">
  <name>Task 2: Criar models Eloquent do domínio</name>
  <files>backend/app/Models/Usuario.php, backend/app/Models/Cupom.php, backend/app/Models/Torneio.php</files>
  <read_first>backend/app/Models/User.php</read_first>
  <action>Criar ou adaptar models em português para o domínio principal, definindo `table`, `fillable`, casts e relacionamentos explícitos quando necessário. O model `Usuario` deve substituir ou alinhar o model padrão do Laravel sem quebrar autenticação. Não usar nomes em inglês para entidades de negócio.</action>
  <verify>Test-Path backend/app/Models/Usuario.php; Test-Path backend/app/Models/Torneio.php</verify>
  <acceptance_criteria>
    - `Usuario`, `Cupom` e `Torneio` existem
    - relacionamentos fundamentais entre usuário, cupom e torneio estão declarados
    - models usam nomes do domínio em português
  </acceptance_criteria>
  <done>Camada Eloquent pronta para autenticação, administração e seeds</done>
</task>

<task type="auto">
  <name>Task 3: Criar seeders para admin e competição mockada</name>
  <files>backend/database/seeders/DatabaseSeeder.php, backend/database/seeders/UsuarioAdministradorSeeder.php, backend/database/seeders/TorneioMockadoSeeder.php</files>
  <read_first>backend/database/seeders/DatabaseSeeder.php</read_first>
  <action>Criar seeders para um administrador inicial, para o torneio mockado e para as regras de pontuação padrão do MVP. O torneio mockado deve popular grupos, seleções, jogadores, fases, rodadas, jogos e regras básicas, de forma suficiente para suportar o desenvolvimento dos módulos seguintes.</action>
  <verify>Select-String -Path backend/database/seeders/TorneioMockadoSeeder.php -Pattern 'regras_pontuacao'</verify>
  <acceptance_criteria>
    - existe seeder de administrador
    - existe seeder do torneio mockado
    - regras de pontuação padrão são inseridas no banco por seed
  </acceptance_criteria>
  <done>Base de dados pode ser recriada e populada de forma determinística</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `php artisan migrate:fresh --seed` executa em `backend/`
- [ ] banco recebe tabelas e dados mockados sem erro
- [ ] models carregam sem erro de namespace ou relacionamento
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Domínio do MVP está definido em código, migrations e seeders
  </success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-do-mvp/01-02-SUMMARY.md`
</output>
