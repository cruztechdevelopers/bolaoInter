---
phase: 05-hardening-de-backend-e-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/Http/Controllers/PainelAdministradorController.php
  - backend/app/Jobs/RecalcularPontuacaoTorneioJob.php
  - backend/tests/Feature/MvpFluxoApiTest.php
autonomous: true
requirements:
  - PONT-05
  - ADM-02
---

<objective>
Mover o recalculo administrativo para a fila do Laravel e encurtar as requests do admin.

Purpose: o fluxo atual recalcula pontuacao do torneio inteiro dentro da request de salvar resultado ou regra, o que viola o hardening esperado da fase.

Output: controller admin persistindo dados e despachando `RecalcularPontuacaoTorneioJob`, com testes cobrindo o despacho da fila.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Despachar job de recalculo apos salvar resultado ou regra</name>
  <files>
    backend/app/Http/Controllers/PainelAdministradorController.php,
    backend/app/Jobs/RecalcularPontuacaoTorneioJob.php,
    backend/tests/Feature/MvpFluxoApiTest.php
  </files>
  <read_first>
    backend/app/Http/Controllers/PainelAdministradorController.php
    backend/app/Services/ServicoPontuacao.php
    backend/tests/Feature/MvpFluxoApiTest.php
    .planning/v1.0-MILESTONE-AUDIT.md
  </read_first>
  <action>
    Criar `backend/app/Jobs/RecalcularPontuacaoTorneioJob.php` com construtor recebendo `int $torneioId`, carregando o torneio atualizado no `handle()` e chamando `ServicoPontuacao->recalcularTorneio($torneio)`. Alterar `PainelAdministradorController` para, nos metodos `salvarResultadoJogo`, `salvarResultadoTorneio` e `atualizarRegraPontuacao`, parar de chamar `recalcularTorneio()` inline e despachar o job via `dispatch()`. Manter a persistencia principal na request e retornar JSON de sucesso sem esperar o processamento pesado. Cobrir em `MvpFluxoApiTest.php` com `Queue::fake()` e asserts de `Queue::assertPushed(RecalcularPontuacaoTorneioJob::class, ...)` para jogo, torneio e regra.
  </action>
  <verify>
    <automated>cd backend && php artisan test --filter=MvpFluxoApiTest</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/Jobs/RecalcularPontuacaoTorneioJob.php` contains `class RecalcularPontuacaoTorneioJob`
    - `backend/app/Jobs/RecalcularPontuacaoTorneioJob.php` contains `public function handle`
    - `backend/app/Http/Controllers/PainelAdministradorController.php` contains `RecalcularPontuacaoTorneioJob`
    - `backend/app/Http/Controllers/PainelAdministradorController.php` does not contain `recalcularTorneio($torneio);`
    - `backend/tests/Feature/MvpFluxoApiTest.php` contains `Queue::fake()`
    - `backend/tests/Feature/MvpFluxoApiTest.php` contains `assertPushed(RecalcularPontuacaoTorneioJob::class`
  </acceptance_criteria>
</task>
</tasks>

<success_criteria>
- Requests administrativas deixam de recalcular o torneio inteiro inline
- Job do Laravel passa a ser o gatilho oficial de recalculo
- Fluxo admin continua respondendo com sucesso apos persistencia
- Existe cobertura automatizada do despacho da fila
</success_criteria>
