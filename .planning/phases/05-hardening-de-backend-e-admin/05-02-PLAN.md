---
phase: 05-hardening-de-backend-e-admin
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - backend/app/Http/Requests/SalvarResultadoJogoRequest.php
  - backend/app/Http/Controllers/PainelAdministradorController.php
  - backend/app/Services/ServicoBracketCupom.php
  - frontend/src/views/AdminPainelView.vue
  - backend/tests/Feature/BracketCupomApiTest.php
  - backend/tests/Feature/MvpFluxoApiTest.php
autonomous: true
requirements:
  - COMP-04
  - ADM-03
  - ADM-04
  - UI-03
---

<objective>
Endurecer o dominio do admin e reduzir acoplamentos perigosos no backend do bracket.

Purpose: hoje o admin aceita classificado invalido em jogo eliminatorio e `ServicoBracketCupom` ainda depende de consultas globais e repetidas que enfraquecem a corretude do dominio.

Output: validacao forte de classificado no backend, admin refletindo apenas opcoes validas e `ServicoBracketCupom` operando com escopo mais forte de torneio.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Validar classificado real do mata-mata no backend admin</name>
  <files>
    backend/app/Http/Requests/SalvarResultadoJogoRequest.php,
    backend/app/Http/Controllers/PainelAdministradorController.php,
    backend/tests/Feature/MvpFluxoApiTest.php
  </files>
  <read_first>
    backend/app/Http/Requests/SalvarResultadoJogoRequest.php
    backend/app/Http/Controllers/PainelAdministradorController.php
    backend/tests/Feature/MvpFluxoApiTest.php
    backend/app/Services/ServicoBracketCupom.php
  </read_first>
  <action>
    Endurecer `SalvarResultadoJogoRequest` e o fluxo do `PainelAdministradorController` para validar que `selecao_classificada_id` em jogo eliminatorio pertence ao par real de participantes daquele confronto. Implementar no backend uma resolucao de participantes reais por jogo administrativo, reutilizando o encadeamento de resultados do proprio torneio. Quando o jogo for eliminatorio e a selecao enviada nao pertencer ao confronto, retornar `422` com mensagem clara. Ajustar `MvpFluxoApiTest.php` para cobrir um caso invalido de classificado em jogo de mata-mata e um caso valido.
  </action>
  <verify>
    <automated>cd backend && php artisan test --filter=MvpFluxoApiTest</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/Http/Requests/SalvarResultadoJogoRequest.php` contains `selecao_classificada_id`
    - `backend/app/Http/Controllers/PainelAdministradorController.php` contains validation path for knockout participants
    - `backend/tests/Feature/MvpFluxoApiTest.php` contains `422`
    - `backend/tests/Feature/MvpFluxoApiTest.php` contains admin result save coverage for knockout game
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduzir escopo global e queries repetidas no ServicoBracketCupom</name>
  <files>
    backend/app/Services/ServicoBracketCupom.php,
    frontend/src/views/AdminPainelView.vue,
    backend/tests/Feature/BracketCupomApiTest.php
  </files>
  <read_first>
    backend/app/Services/ServicoBracketCupom.php
    frontend/src/views/AdminPainelView.vue
    backend/tests/Feature/BracketCupomApiTest.php
    .planning/v1.0-MILESTONE-AUDIT.md
  </read_first>
  <action>
    Refatorar `ServicoBracketCupom` para parar de resolver fases/jogos por `slug` global fora do contexto do torneio. Construir mapas internos a partir do `Torneio` carregado em `gerar()`, incluindo jogos por fase e referencias por ordem, e reutilizar esses mapas tanto em `resolverJogoIdPorOrigem()` quanto em `faseBloqueadaNoCupom()`. Remover a dependencia de queries repetidas de `Fase::query()` e `Jogo::query()` dentro da derivacao do bracket. Ajustar `AdminPainelView.vue` para que o select de classificado no mata-mata mostre apenas participantes validos do confronto atual em vez de todas as selecoes. Expandir `BracketCupomApiTest.php` para garantir que o bracket continua sendo derivado corretamente apos o refactor.
  </action>
  <verify>
    <automated>cd backend && php artisan test --filter=BracketCupomApiTest</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/Services/ServicoBracketCupom.php` does not contain `Fase::query()->where('slug'`
    - `backend/app/Services/ServicoBracketCupom.php` contains tournament-scoped map reuse for bracket derivation
    - `frontend/src/views/AdminPainelView.vue` does not contain `return selecoes.value` inside `opcoesClassificado`
    - `backend/tests/Feature/BracketCupomApiTest.php` exits 0
  </acceptance_criteria>
</task>
</tasks>

<success_criteria>
- Backend rejeita classificado invalido em jogo eliminatorio
- Admin mostra opcoes coerentes com o confronto sem depender disso como fonte de verdade
- ServicoBracketCupom deixa de depender de consultas globais por slug
- Regressao do bracket segue coberta por teste
</success_criteria>
