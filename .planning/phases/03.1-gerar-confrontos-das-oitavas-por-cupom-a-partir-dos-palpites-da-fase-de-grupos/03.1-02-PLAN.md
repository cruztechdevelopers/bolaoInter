---
phase: 03.1-gerar-confrontos-das-oitavas-por-cupom-a-partir-dos-palpites-da-fase-de-grupos
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - frontend/src/tipos.ts
  - frontend/src/views/CupomView.vue
autonomous: true
requirements:
  - CUP-05
  - APO-04
  - APO-05
  - UI-02
---

<objective>
Fazer a `CupomView` consumir o bracket derivado do cupom em vez dos jogos eliminatorios estaticos do torneio.

Purpose: o backend pode gerar os confrontos corretamente e ainda assim o usuario continuar vendo participantes estaticos do seed se a view seguir renderizando `torneio.jogos` como fonte unica do mata-mata.

Output: tipos de bracket previstos adicionados ao frontend, carregamento do endpoint `/cupons/{id}/bracket`, renderizacao da `Round of 32` e das eliminatorias seguintes com participantes previstos do cupom e resumo de podio derivado dessa mesma estrutura.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Integrar CupomView ao endpoint de bracket do cupom</name>
  <files>
    frontend/src/tipos.ts,
    frontend/src/views/CupomView.vue
  </files>
  <action>
    Em `frontend/src/tipos.ts`, adicionar tipos para o payload `{ bracket: [...] }`. Em `frontend/src/views/CupomView.vue`, carregar `/cupons/{id}/bracket` junto com cupom e apostas; separar jogos de grupos de confrontos eliminatorios previstos; substituir leituras de `torneio.value.jogos` nas eliminatorias por `jogosEliminatoriosDoCupom`; usar os participantes previstos para cards, navegacao de fases, `Round of 32`, `autoSalvar`, `preencherFormulario` e `resumoBracket`; e manter os horarios do jogo base para fechamento e exibicao.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
</task>
</tasks>

<success_criteria>
- CupomView deixa de depender de participantes eliminatorios estaticos do torneio
- O mata-mata mostra `Round of 32` e confrontos seguintes previstos por cupom
- Resumo de campeao, vice e terceiro deriva do mesmo bracket do cupom
- Build do frontend continua verde
</success_criteria>
